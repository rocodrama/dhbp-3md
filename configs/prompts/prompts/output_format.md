Respond in exactly the following structure. Write nothing else:

```
REASONING:
<clinical reasoning, at most 6 sentences>

ANSWER: <answer>
```

Rules:

1. The final line must begin with `ANSWER: ` followed by {answer_spec}.
2. Put only the answer value on that line.
3. Do not restate the option text, add punctuation, or explain your choice on the `ANSWER:` line.
4. Write nothing after the `ANSWER:` line.
5. Never omit the `ANSWER:` line. A response without it is scored as incorrect.
6. Finish `REASONING:` within the sentence limit so the final answer line is not truncated.

